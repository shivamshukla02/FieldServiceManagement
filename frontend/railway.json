{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "watchPatterns": ["src/**"]
  },
  "deploy": {
    "startCommand": "npm run preview -- --host --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}